{
  "name": "AA Efficiency Dashboard",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.12-bookworm",
  "forwardPorts": [8765],
  "portsAttributes": {
    "8765": {
      "label": "AA Efficiency Dashboard",
      "onAutoForward": "openBrowser"
    }
  },
  "postCreateCommand": "python -m pip install -r requirements.txt && sudo -n python -m playwright install-deps chromium && python -m playwright install chromium",
  "postStartCommand": "python -m playwright install chromium && (pkill -f 'python app.py' || true) && nohup python app.py > /tmp/aa-dashboard.log 2>&1 &",
  "remoteUser": "vscode"
}
